Sopinet/template-landing-bundle
==============================

Bundle for Symfony2 with Landing template

Installation
============

```yaml
{
    "require": {
        "sopinet/template-landing-bundle": "0.0.1"
    }
}
```

With composer require:

composer require sopinet/template-landing-bundle:0.0.1
